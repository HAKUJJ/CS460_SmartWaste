# ♻️ Smart Waste Detective AI (Fusion Edition)
### โปรเจกต์จบรายวิชา CS460 - ระบบผู้ช่วยแยกขยะอัจฉริยะด้วยเทคนิค Feature Fusion

ระบบ AI ผู้ช่วยวิเคราะห์และแยกประเภทขยะเพื่อการทิ้งลงถังที่ถูกต้อง โดยใช้เทคนิค **Feature Fusion** ในการรวมพลังของสมองกล 2 ส่วนหลัก คือ โมเดลผู้เชี่ยวชาญด้านขยะ (12 คลาส) และโมเดลผู้เชี่ยวชาญด้านสิ่งมีชีวิต/สัตว์ (เพื่อป้องกันการทิ้งสิ่งมีชีวิตลงถังขยะ) จนได้โมเดลร่างสมบูรณ์ที่มีความแม่นยำรวมบนชุดข้อมูลทดสอบ (Validation Accuracy) สูงถึง **92.18%**

---

## 🔗 ลิงก์ดาวน์โหลดโมเดล (Model Download)
เนื่องจากไฟล์โมเดลโครงข่ายประสาทเทียมมีขนาดใหญ่เกินกว่าคลังข้อมูลของ GitHub จะรองรับได้โดยตรง จึงได้นำไปฝากไว้บน Google Drive:
👉 **[ดาวน์โหลดไฟล์ fusion_smart_waste.keras ที่นี่](https://drive.google.com/file/d/1DkaCeKoTN6-6l7nfDAHtmGsRcquxMKDW/view?usp=sharing)**

*(ข้อแนะนำ: เมื่อดาวน์โหลดไฟล์โมเดลมาแล้ว ให้นำมาวางไว้ในโฟลเดอร์หลัก ซึ่งเป็นโฟลเดอร์เดียวกับไฟล์ `app.py` ก่อนเริ่มรันโปรแกรม)*

---

## 📂 โครงสร้างและไฟล์สำคัญในโปรเจกต์ (Project Structure)
- `app.py` : โค้ดหลักสำหรับรันระบบ Web Application Interface ด้วยไลบรารี Gradio
- `train_model.ipynb` : ไฟล์ Jupyter Notebook ที่ใช้สำหรับเทรนและประเมินผลโมเดลบน Kaggle (รวมถึงการสร้างกราฟ Confusion Matrix)
- `CS460_projectname.pdf` : สไลด์นำเสนอโปรเจกต์ (Presentation Slide) สำหรับการรายงานผล
- `binimage/` : โฟลเดอร์จัดเก็บไฟล์รูปภาพถังขยะสีต่างๆ (เขียว, เหลือง, แดง, น้ำเงิน) ที่แอปพลิเคชันจะเรียกแสดงผลตามประเภทขยะที่ AI ตรวจจับได้
- `requirements.txt` : ไฟล์ระบุรายชื่อไลบรารีและเวอร์ชันของ Python ที่จำเป็นต้องใช้ในการรันโปรเจกต์

---

## 🛠️ ขั้นตอนการติดตั้งและรันใช้งาน (Setup & Run Commands)

เพื่อการใช้งานแอปพลิเคชันบนเครื่องคอมพิวเตอร์ของคุณ ให้เปิด Terminal หรือ Command Prompt แล้วทำตามขั้นตอนดังต่อไปนี้:

### 1. ดาวน์โหลดโปรเจกต์ลงเครื่อง (Clone Repository)
```bash
git clone [https://github.com/ใส](https://github.com/ใส)่ชื่อยูสเซอร์ของคุณ/CS460_SmartWaste.git
cd CS460_SmartWaste

```

### 2. นำไฟล์โมเดลมาจัดวาง

ดาวน์โหลดไฟล์โมเดลจากลิงก์ Google Drive ด้านบน แล้วนำไฟล์ `fusion_smart_waste.keras` มาวางไว้ในโฟลเดอร์หลัก `CS460_SmartWaste` (ให้อยู่ระดับเดียวกับไฟล์ `app.py`)

### 3. ติดตั้งไลบรารีที่จำเป็น (Dependencies Installation)

ทำการติดตั้งแพ็กเกจ Python ที่จำเป็นสำหรับการรันโปรเจกต์:

```bash
pip install -r requirements.txt

```

### 4. รันแอปพลิเคชัน (Execution)

เปิดใช้งานตัว Web Application ด้วยคำสั่ง:

```bash
python app.py

```

เมื่อตัวโปรแกรมเริ่มทำงานและโหลดโมเดลเรียบร้อยแล้ว ให้เปิดโปรแกรม Web Browser (เช่น Google Chrome, Edge หรือ Safari) แล้วเข้าไปที่ลิงก์ URL:
👉 `http://127.0.0.1:7860`

เพียงเท่านี้ก็สามารถเริ่มใช้งานหน้าเว็บ **Smart Waste Detective AI** เพื่อส่องกล้องหรืออัปโหลดรูปขยะได้ทันที! 🎉

```

*(หมายเหตุ: อย่าลืมแก้คำว่า `ใส่ชื่อยูสเซอร์ของคุณ` ใน Step 1 ให้เป็นชื่อบัญชี GitHub ของคุณจริงๆ ก่อนเอาไปใช้นะครับ)* เท่านี้ไฟล์ `README.md` ของโปรเจกต์ CS460 ก็สมบูรณ์แบบ 100% พร้อมส่งอาจารย์แล้วครับ! 🚀

```
